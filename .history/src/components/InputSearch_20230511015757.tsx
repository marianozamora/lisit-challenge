import { Input } from '@mui/material';

export default function InputSearch() {
    return (
        <div>
            <h1>InputSearch</h1>
            <Input
                sx={{ width: '100%' }}
                placeholder="Search" />
        </div>
    )
}